I forgot to tag the repo with which version of BetterThanBatch (BTB lang) it was using.

Very problematic since the latest version doesn't work. It's possible to update the code
but BTB is deprecated in favour of Basin so this code is dead (I will be translating it to Basin before removing it.)
